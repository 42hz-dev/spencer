name: Spencer CI/CD

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    name: Build Spencer
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install Spencer-material

      - name: Build Documentation
        run: |
          Spencer build --strict

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Spencer-site
          path: site/
          retention-days: 7

  deploy:
    name: Deploy Spencer
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: Spencer-site
          path: site/

      - name: Start SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Register Known Hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ vars.HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to Server
        run: |
          rsync -az --delete \
            site/ \
            ${{ vars.USERNAME }}@${{ vars.HOST }}:/var/www/spencer/

      - name: Verify Deployment
        run: |
          ssh ${{ vars.USERNAME }}@${{ vars.HOST }} \
            "echo 'Deploy Success' && ls -lah /var/www/spencer | head"